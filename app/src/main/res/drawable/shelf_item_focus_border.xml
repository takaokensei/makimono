<?xml version="1.0" encoding="utf-8"?>
<!--
    D-pad focus ring for a photo-backed shelf card: a cyan outline only, so
    the preview thumbnail underneath stays fully visible. rail_item_focus_bg
    is a solid fill made for the icon-only nav rail pills and would blot out
    the whole photo if reused here.
-->
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:state_focused="true">
        <shape android:shape="rectangle">
            <solid android:color="@android:color/transparent" />
            <stroke
                android:width="3dp"
                android:color="?attr/colorAccentPrimary" />
            <corners android:radius="12dp" />
        </shape>
    </item>
    <item android:state_pressed="true">
        <shape android:shape="rectangle">
            <solid android:color="@android:color/transparent" />
            <stroke
                android:width="3dp"
                android:color="?attr/colorAccentPrimary" />
            <corners android:radius="12dp" />
        </shape>
    </item>
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@android:color/transparent" />
            <corners android:radius="12dp" />
        </shape>
    </item>
</selector>
